# CLAUDE.md

このファイルは、このリポジトリ(ComposePilot)固有のルールです。
ユーザー共通の `~/.claude/CLAUDE.md` に記載がある場合はそちらの内容も適用されますが、
このリポジトリを扱う際は、以下のルールを追加で必ず守ってください。

## 背景

このリポジトリはGitHub(`https://github.com/tofu76/composepilot-for-claude-code`)で
公開する前提のOSSです。過去に、開発メモとしてのコメント・コミットメッセージへ
無関係な私的プロジェクト名(例: 開発者の別の非公開リポジトリ名)が実測値の引用として
紛れ込んだことがあります。**このリポジトリはいずれ他人が読む前提で扱うこと。**

## 個人情報・環境固有情報を混入させないためのルール

- 実名・個人のメールアドレス・電話番号など、直接個人を特定できる情報をソースコード・
  ドキュメント・コミットメッセージ・スクリプトに書かない。説明用の値が必要な場合は
  プレースホルダ(`<name>`、`<Apple ID>`等、`docs/GUIDE.md`の既存の書式を踏襲)を使う。
- Apple Developer Team ID・証明書のCommon Name(実名を含む)を平文で書かない。
  `COMPOSEPILOT_SIGN_IDENTITY`等の環境変数経由でのみ扱う(`Scripts/build_app.sh`の方式を踏襲)。
- ユーザーの他の(この`ComposePilot`とは無関係な)プロジェクト名・リポジトリ名を、
  コメント・ドキュメント・コミットメッセージ中の実測値の引用や具体例として書かない。
  実例が必要な場合は架空の名前(例: `MyProject`)に置き換える。
- `~/Library/Application Support/ComposePilot/`配下に実行時生成される診断・調査ファイル
  (`status.txt`、`focus.log`、`inspections/*.json`、`known_good_signature.json`)は
  リポジトリの外に置かれる設計を維持し、絶対にリポジトリ内へコピー・コミットしない。
  実データのサンプルが必要な場合は値を伏せて構造だけを示す。
- ホームディレクトリの絶対パス(`/Users/<username>/...`)をコード・スクリプト・
  ドキュメントにハードコードしない。`$HOME`・`~`、または`Bundle.main`等の実行時解決を使う。
- 新しいファイルを追加する前に、実際のスクリーンショット・ログ・調査結果など
  「個人の使用実態が写り込んだファイル」でないかを確認する。
- git履歴を書き換える(`rebase -i`、`filter-branch`、`filter-repo`、force-push等)場合は、
  実行前に必ずユーザーへ確認する(通常の安全原則どおりだが、公開リポジトリでは
  影響が大きいためこのリポジトリでは特に明記する)。

## 機械的な検出の仕組み(pre-commitフック)

文書化されたルールだけでは「うっかり」を防げないため、コミット時に自動でブロックする
仕組みを用意している。

- `Scripts/git-hooks/pre-commit`: ステージされた変更の**追加行**に対し、汎用パターン
  (ホームディレクトリ絶対パス、個人向けメールドメイン、証明書CNへの実名混入の形)を
  検出したらコミットを中止する。このファイルは公開リポジトリに含まれるため、
  実名・実メールアドレス・実ユーザー名などの具体的な値は絶対に書かない。
- `Scripts/git-hooks/personal-terms.local`: 上記の汎用パターンでは拾えない、
  実名・実メールアドレス・このマシン固有のユーザー名・無関係な私的プロジェクト名などの
  「具体的な文字列」を1行1件で列挙するローカル限定ファイル。`.gitignore`で除外されており
  **絶対にコミットしない**。新しい環境で作業する場合は、このファイルが存在しないと
  汎用パターン以外のチェックが効かないため、必要な用語を都度追記すること。
- **有効化**: 新しくクローンした、またはこのリポジトリで初めて作業する場合は、まず
  `git config core.hooksPath Scripts/git-hooks` を実行してフックを有効化してから
  コミットすること(`core.hooksPath`はリポジトリのクローンごとに必要なローカル設定であり、
  gitの管理下にある`.git/hooks/`とは別物)。
- フックが誤検知でブロックした場合は、内容を確認したうえで
  `Scripts/git-hooks/pre-commit`または`personal-terms.local`のパターンを調整する。
  `git commit --no-verify`によるバイパスは、このフックの目的(個人情報混入の防止)に
  反するため原則として使わない。
- 新しいカテゴリの個人情報混入パターンに気づいた場合は、このファイルへのルール追記と
  同時に、汎用パターンなら`Scripts/git-hooks/pre-commit`へ、個別の具体的な値なら
  `personal-terms.local`へ追加し、仕組みとして再発防止する(文書だけの追記で終わらせない)。
- このフックの検出パターンは、過去に実際に見つかった漏えい事例を土台にした固定リストであり、
  未知の新しい個人情報を検出できるとは限らない。公開・force-push等の前には、フックに
  加えて目視での確認も行うこと。

## コミット時のメールアドレス

このリポジトリのコミットは、個人のメールアドレスではなくGitHubのnoreplyアドレス
(`git config user.email`で確認できる、このリポジトリのローカル設定)を使うことを前提とする。
何らかの理由で個人のメールアドレスに戻っていた場合は、コミット前に修正すること。

## リリース公開時の確認

- `Scripts/publish_release.sh`(gitタグのpushとGitHub Releaseの作成)を実行する前には、
  実行前に必ずユーザーへ確認する(force-pushと同様、リモート・公開状態への影響が
  大きい操作のため)。
- リリース用のDMG/pkgなどのビルド生成物はgitに追跡させない(`.build/`は`.gitignore`対象の
  まま維持する)。配布はGitHub Releasesへのアセット添付で行う。
